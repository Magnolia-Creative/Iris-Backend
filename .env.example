IRIS_ENV=local

# Required locally. Keep real credentials in .env, which is gitignored.
DATABASE_URL=

# Optional override when you need Clerk to differ from IRIS_ENV.
# CLERK_INSTANCE=local

# Local development only. Ignored unless IRIS_ENV is local/development/dev/test.
# LOCAL_AUTH_BYPASS=true
# LOCAL_AUTH_BYPASS_USER_ID=local_dev_user
